package org.yuelao.framework.oauth.authentication.token.password;

import org.yuelao.framework.oauth.authentication.token.OAuth2AccessToken;
import org.yuelao.framework.oauth.authentication.token.OAuth2RefreshToken;

/**
 * 返回的 access token
 */
public class UserNamePasswordAccessToken {
	
	private OAuth2AccessToken accessToken;
	
	private OAuth2RefreshToken refreshToken;
	
	
}
